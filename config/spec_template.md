# Module Specification: {{MODULE_NAME}}

## Overview
{{MODULE_OVERVIEW}}

## Parameters
| 이름         | 타입     | 기본값 | 설명                 |
|--------------|----------|--------|----------------------|
| `EXAMPLE_PARAM` | `int`    | `32`   | 예시 파라미터        |
|              |          |        |                      |

## Inputs
| 이름 (`i_` 접두사) | 폭 (`WIDTH` 등 사용 가능) | 타입 (`input logic`) | 설명                       |
|--------------------|---------------------------|----------------------|----------------------------|
| `i_clk`            | `1`                       | `input logic`        | 주 클럭 신호               |
| `i_resetn`         | `1`                       | `input logic`        | 동기식 액티브 로우 리셋    |
|                    |                           |                      |                            |

## Outputs
| 이름 (`o_` 접두사) | 폭                         | 타입 (`output logic`) | 설명                       |
|--------------------|----------------------------|-----------------------|----------------------------|
| `o_example`        | `1`                        | `output logic`        | 예시 출력 신호             |
|                    |                            |                       |                            |

## Functional Description
{{FUNCTIONAL_DESCRIPTION}}

*Note: LLM은 위 테이블 형식을 반드시 준수하여 파라미터, 입력, 출력 정보를 정확하게 작성해야 합니다. 해당 사항이 없으면 테이블 내용은 비워두세요. `{{PLACEHOLDER}}` 부분은 사용자 요구사항에 맞게 채워주세요.* 